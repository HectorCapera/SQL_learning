SELECT MAX(age) FROM users;

SELECT MIN(age) FROM users;


/* MAX es una función de agregación que se utiliza para calcular el valor máximo de un conjunto de valores */ 

/* MIN es una función de agregación que se utiliza para calcular el valor mínimo de un conjunto de valores */ 